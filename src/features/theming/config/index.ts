import type { ThemeKey } from '../types'

export const DEFAULT_THEME_KEY: ThemeKey = 'default'
export const LOCAL_STORAGE_THEME_KEY = 'app-active-theme-key'
export const LOCAL_STORAGE_DARK_MODE_KEY = 'app-dark-mode-enabled'

export { themeDefinitions } from './definitions'
